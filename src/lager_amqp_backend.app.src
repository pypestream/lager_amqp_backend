{application, lager_amqp_backend, [
  {description, "Lager AMQP Logging Backend"},
  {vsn, "1.0.0"},
  {registered, []},
  {applications, [
    kernel,
    stdlib,
    lager,
    amqp_client
  ]},
  {mod, { lager_amqp_backend_app, []}},
  {env, []}
]}.
